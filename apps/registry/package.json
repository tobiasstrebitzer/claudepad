{
  "name": "@claudepad/registry",
  "version": "0.14.1",
  "private": true,
  "description": "Reference implementation of the claudepad registry contract (registry-spec.md): a Cloudflare Worker (R2 blobs/sessions + KV directory) built on a storage-agnostic handler so it runs against an in-memory backend in tests. Zero-knowledge blob store + identity directory + opt-in trusted sessions.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "build": "tsc -p tsconfig.json",
    "prepack": "pnpm clean && pnpm build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "lint": "eslint ."
  },
  "dependencies": {
    "@claudepad/registry-spec": "workspace:*",
    "@claudepad/crypto": "workspace:*"
  }
}

# Claude-Card generator

- Generates a score-card based on performance (new reports).
- Easy to share on social media
- Key "vanity" metrics, like total tokens and cost, but grounded in "metrics that matter".
- E.g. "cache-ratio", or other efficiency-related metrics (e.g. average context size, session reset discipline, etc...)

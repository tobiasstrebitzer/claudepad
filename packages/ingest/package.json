{
  "name": "@claudepad/ingest",
  "version": "0.6.0",
  "private": true,
  "description": "Pure, isomorphic ingest helpers (PRD-04): payload shape detection, OS-aware onboarding paths, and file-size caps. Zero runtime dependencies; shared by the web client and (later) the CLI.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}

{
  "name": "@claudepad/registry-spec",
  "version": "0.8.0",
  "private": true,
  "description": "The open registry contract (REGISTRY-SPEC.md): provider interfaces (store + identity directory + opt-in trusted mode), wire DTOs, endpoint constants, the TLS-only guard, a tolerant manifest parser, and the OpenAPI document. Zero runtime dependencies; shared by the client SDK and any conformant registry.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    },
    "./openapi": {
      "types": "./src/openapi.ts",
      "import": "./src/openapi.ts"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}

{
  "name": "@claudepad/secrets",
  "version": "0.8.0",
  "private": true,
  "description": "Client-side secret detection + redaction (PRD-06): prefix/entropy/.env scanners and a redactor that splits a session into a placeholder body + an encrypted secret map. Pure, isomorphic, zero runtime dependencies.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@claudepad/schema": "workspace:*"
  }
}
